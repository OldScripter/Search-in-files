#include "../include/SearchServer.h"

std::vector<std::vector<RelativeIndex>> SearchServer::search(const std::vector<std::string>& queries_input)
{
    for (auto word : queries_input)
    {
        // Get the uniques words from input


        /* std::pair<std::string, RelativeIndex> result;
        RelativeIndex relativeIndex;
        relativeIndex.doc_id = {0};
        relativeIndex.rank = {0};

        result.first = word;
        result.second = relativeIndex;

        auto emplace_result = answer.emplace(result);
        if (emplace_result.second)
        {
            
            int word_count = 0;
            auto entries = _index.getWordCount(word);
            for ()
        } */
    }
}